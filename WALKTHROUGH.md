# Portfolio walkthrough — talking points

Live site: **https://whitgai.github.io/portfolio/**
Audience: sharp peers / like-minded builders. Time: ~3–5 min.

---

## The frame (open with this)
"Everything here I built idea-to-shipped on the Claude stack — the site itself included. The thesis is one line: **the LLM extracts, tested code decides.** The model reads messy input and orchestrates; anything that has to be exact lives in deterministic, tested code. That's the difference between a demo that drifts and something people actually trust."

## Hero
"Frontier AI built around people. Fifteen years running enterprise AI inside a Fortune 500 — 91% daily adoption in 90 days, audited. Now I build founder-direct. Five shipped systems, all live from here. Let me show you."

## The differentiator (say this BEFORE the demos — it's the moat)
"Most AI products put the important logic in a prompt and hope. I don't. Scores, rules, CRM mappings — those are tested code the model calls. So it can't hallucinate a score or a price. That one discipline is what makes any of this safe to put in front of a customer."

## Work — walk the four, click into two

**Staffing OS** — the commercial wedge.
"This isn't a prettier Bullhorn — it's a *verified talent layer over* the ATS you already run. The candidate owns a profile they keep true; your database self-cleans on consent. That's what Bullhorn + Copilot structurally can't do."
→ Click **Enter the live demo** → recruiter desk → the holographic **Talent Passport**. "Each surface its own world, one front door."

**CourtVision AI** — the wow moment.
→ Click **Open the app → Match → flip Auto-watch ON.**
"Watch — it calibrates the court, then keeps score *itself*: sees the bounce, calls the line, the scoreboard moves on its own. And the score isn't guessed — it's a USA-Pickleball side-out engine I unit-tested, 14 of 14. Vision detects the event; tested code keeps the score."
Coach Mode in one line: "Same brand, async — watches a clip, gives one or two specific fixes, not ten."

**Recruit Ops** — the proof it generalizes.
"An AI recruiting desk: intake from Slack or Teams, documented and matched, written back to the CRM — one MCP server. The match score is tested code, so it shows its work."

**Munch Quest** — the proof the method is *mine*, not just my job.
"And it's not only enterprise. I built this one for my two boys — a game that turns picky eating into a quest: build a balanced rainbow plate, climb a tasting ladder, one brave bite at a time. Same discipline as everything else here — the surface is fun, but the scoring is a tested engine, 99 assertions green. Offline, no login, built in an afternoon. That's the tell: the pattern's so repeatable I reach for it on a Saturday for my kids."
→ Click **Open the app** → build a plate → watch the balance meter flip green. "Eat-the-rainbow as a win condition, not a lecture."

## Systems — "how I think" (the part peers care about)
"This is the actual engine the projects are proof of: one tested core, many surfaces. A new front-end is wiring, not a rebuild — CourtVision's browser scorekeeper is the *same* logic as the MCP, verified 1:1. New CRM? A new adapter file, not a rewrite. That's why each build is faster than the last — idea to live in about a day."

## The meta-point (land this — your edge with like-minded folks)
"The reason I can show you five polished systems — enterprise to a kids' game — isn't a big team. It's one repeatable method: engine, surface, tests, spec, on the Claude stack. Each build reuses the last one's bones, so the domain logic is the only new part — that's why they ship in about a day. The capability is the product; the apps are just the proof it travels. This whole site was vibe-coded the same way."

## Close / the ask
"That's the pitch: raw idea to shipped, tested, trustworthy AI — fast. If that's useful to you or someone you know, the whole thing's one link." → share **whitgai.github.io/portfolio**

---

## If they ask…
- **"Real or a mockup?"** → "Repos are public, installable, tested, CI-wired. Demos run on synthetic data — no former-employer IP — but the logic is real code."
- **"How long did this take?"** → "Days, not months. The pattern's already decided; only the domain logic is new."
- **"What's the business?"** → "Staffing OS is the commercial wedge. The rest proves the method travels across domains."
- **"Why trust the score/call?"** → "It's not the model deciding — it's tested code the model calls. (Show the 14/14.)"

## Demo cheat-sheet (what to click for impact)
1. CourtVision → Match → **Auto-watch ON** — the scoreboard moving itself is the hook.
2. Staffing OS → **Talent Passport** — most visually striking surface.
3. Munch Quest → build a plate → **balance meter flips green** — the range proof; lands warm with anyone who has kids.
4. Technical room? Type **`gai`** on the homepage → "chaos magic." Signals it's hand-built, not a template.
